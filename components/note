"use client";

import React from "react";
import { motion } from "framer-motion";
import { Zap, Cpu, Globe, ExternalLink, Layers } from "lucide-react";
import { cn } from "@/components/lib/utils";

const ProjectCard = ({ 
  title, 
  description, 
  tags, 
  className, 
  icon: Icon, 
  isFeatured = false 
}: { 
  title: string; 
  description: string; 
  tags: string[]; 
  className?: string; 
  icon: any; 
  isFeatured?: boolean 
}) => (
  <motion.div
    whileHover={{ y: -5 }}
    className={cn(
      "group relative flex flex-col justify-between overflow-hidden rounded-3xl border border-white/10 bg-zinc-900/50 p-6 transition-all duration-500 hover:border-blue-500/30 hover:bg-zinc-900/80",
      isFeatured && "md:col-span-2 md:row-span-2 bg-gradient-to-br from-zinc-900 via-zinc-900 to-black",
      className
    )}
  >
    {/* Blue Glow Effect on Hover */}
    <div className="absolute -inset-px rounded-3xl bg-gradient-to-br from-blue-500/20 to-transparent opacity-0 transition-opacity duration-500 group-hover:opacity-100" />
    
    <div className="relative z-10">
      <div className={cn(
        "mb-4 flex h-12 w-12 items-center justify-center rounded-2xl border border-white/10 bg-white/5 transition-colors group-hover:border-blue-500/50 group-hover:bg-blue-500/10",
        isFeatured && "h-14 w-14 border-blue-500/20 bg-blue-500/5 text-blue-400"
      )}>
        <Icon size={isFeatured ? 28 : 22} strokeWidth={1.5} />
      </div>
      
      <h3 className={cn("font-bold tracking-tight text-white", isFeatured ? "text-3xl uppercase italic" : "text-xl")}>
        {title}
      </h3>
      <p className="mt-2 text-sm leading-relaxed text-zinc-400 group-hover:text-zinc-300">
        {description}
      </p>
    </div>

    <div className="relative z-10 mt-6 flex items-center justify-between">
      <div className="flex flex-wrap gap-2">
        {tags.map((tag) => (
          <span key={tag} className="rounded-md border border-white/5 bg-white/5 px-2 py-1 text-[10px] font-mono text-zinc-500 uppercase">
            {tag}
          </span>
        ))}
      </div>
      <div className="flex gap-3 text-zinc-500 group-hover:text-blue-400">
        {/* <Github size={18} className="cursor-pointer hover:scale-110 transition-transform" /> */}
        <ExternalLink size={18} className="cursor-pointer hover:scale-110 transition-transform" />
      </div>
    </div>
  </motion.div>
);

export default function ProjectsSection() {
  return (
    <section id="projects" className="relative px-6 py-24 md:px-16">
      <div className="mb-16 flex flex-col md:flex-row md:items-end md:justify-between gap-4">
        <div>
          <div className="flex items-center gap-2 text-blue-500 mb-2">
            <Layers size={16} />
            <span className="text-xs font-mono uppercase tracking-[0.3em]">Project_Archive</span>
          </div>
          <h2 className="text-4xl md:text-6xl font-black tracking-tighter text-white uppercase italic">
            Selected <span className="text-zinc-800 not-italic">Works</span>
          </h2>
        </div>
        <p className="max-w-xs text-xs font-mono text-zinc-500 leading-relaxed uppercase">
          // A collection of hardware logic, web architecture, and startup ventures.
        </p>
      </div>

      <div className="grid grid-cols-1 gap-4 md:grid-cols-3 md:auto-rows-[220px]">
        {/* MAIN FEATURE: THECLA */}
        <ProjectCard
          isFeatured
          title="Thecla"
          description="A scalable ed-tech ecosystem designed for African developers. Built with a focus on performance, accessibility, and high-level engineering standards."
          tags={["Next.js", "TypeScript", "AWS", "Prisma"]}
          icon={Zap}
        />

        {/* EEE HARDWARE PROJECT */}
        <ProjectCard
          title="Signal Logic"
          description="MATLAB-based signal processing system for analyzing electrical load distributions in real-time."
          tags={["MATLAB", "DSP", "Signal Processing"]}
          icon={Cpu}
        />

        {/* OPEN SOURCE / WEB3 */}
        <ProjectCard
          title="Web3 Connect"
          description="Exploring decentralized protocols and building lightweight wrappers for cross-chain identity."
          tags={["Solidity", "Ethers.js", "Web3"]}
          icon={Globe}
        />

        {/* CTA CARD */}
        <motion.div 
          whileHover={{ scale: 0.99 }}
          className="md:col-span-3 flex cursor-pointer items-center justify-between rounded-3xl bg-blue-600 p-8 transition-transform active:scale-95 group overflow-hidden relative"
        >
          <div className="absolute inset-0 bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-10" />
          <div className="relative z-10">
            <h3 className="text-2xl md:text-4xl font-black uppercase italic text-white leading-none">
              Start a new project?
            </h3>
            <p className="text-blue-200 mt-2 font-mono text-xs tracking-widest uppercase opacity-80 group-hover:opacity-100">
              [ Click to initiate conversation ]
            </p>
          </div>
          <div className="relative z-10 rounded-full bg-black p-4 text-white shadow-2xl transition-transform group-hover:rotate-45">
            <ExternalLink size={24} />
          </div>
        </motion.div>
      </div>
    </section>
  );
}